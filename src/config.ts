import type { CrmConfig } from "./types";

// Mirrors the Design Component's editable "props" panel from the prototype.
export const config: CrmConfig = {
  advisorName: "Dana Ferreira, CFP",
  // Signed off on appointment confirmation emails.
  advisorEmail: "dana.ferreira@meridianadvisors.com",
  firmName: "Meridian Bank",
  productName: "Financial advisors · CRM",
  defaultScreen: "calendar",
  showSaturday: false,
  currency: "USD",
};
